import { ScrolToBottomDirective } from './scrol-to-bottom.directive';

describe('ScrolToBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolToBottomDirective();
    expect(directive).toBeTruthy();
  });
});
