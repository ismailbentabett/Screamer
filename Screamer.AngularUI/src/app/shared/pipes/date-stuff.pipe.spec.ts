import { DateStuffPipe } from './date-stuff.pipe';

describe('DateStuffPipe', () => {
  it('create an instance', () => {
    const pipe = new DateStuffPipe();
    expect(pipe).toBeTruthy();
  });
});
